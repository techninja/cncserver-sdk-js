cncserver-sdk-js
================

The web specific JavaScript SDK wrapper for the CNC Server API.

**This is brand new and must be ported from RoboPaint, not to mention CNC Server has to support cross domain access**
